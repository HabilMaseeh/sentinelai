# SentinelAI Frontend

React + Vite UI for the SentinelAI SOC demo.

**Run locally**
```bash
npm install
npm run dev
```

Backend expected at `http://localhost:8000`.
Frontend runs at `http://localhost:5173`.
